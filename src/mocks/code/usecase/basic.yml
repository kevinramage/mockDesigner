name: helloWorldMock
services:
- name: helloWorldService
  method: GET
  path: /api/v1/helloService
  response:
    triggers:
    - type: none
      actions:
      - type: message
        status: 200
        body: "Hello world {{.request.query.username}}"