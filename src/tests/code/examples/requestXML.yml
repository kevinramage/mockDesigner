name: MyMock
services:
  - name: "myService"
    method: POST
    pingPath: /api/v1/myService
    path: /api/v1/myService/:id/subItem/:subId
    response:
      triggers:
      - type: none
        actions:
        - type: message
          status: 200
          headers:
            content-type: application/xml
          bodyFile: ../responses/examples/request.xml