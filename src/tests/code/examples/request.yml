name: MyMock
services:
  - name: "myService"
    method: POST
    path: /api/v1/myService/:id/subItem/:subId
    response:
      triggers:
      - type: none
        actions:
        - type: message
          status: 200
          headers:
            content-type: application/json
          bodyFile: ../responses/request.json